# 🧪 Инструкция по тестированию улучшений

## ✅ Что изменилось:

### 1. **Модель AI**
- **Было:** `google/gemini-2.5-flash-lite` (слабая модель)
- **Стало:** `openai/gpt-4o` (топовая модель GPT-4)
- **Результат:** Намного лучше следует инструкциям

### 2. **Temperature**
- **Было:** 0.3
- **Стало:** 0.0 (минимум для детерминизма)
- **Результат:** Более точное следование промптам

### 3. **Промпты**
- ✅ Добавлены СТРОГИЕ правила с эмодзи и разделителями
- ✅ Множество примеров правильных и неправильных ответов
- ✅ Явное требование: "информация отсутствует" без объяснений
- ✅ Четкая разница между "согласовано" и "ждем ОС"

### 4. **Форматирование**
- ✅ Блоки с "информация отсутствует" группируются в раздел "📋 Нет информации"

## 🚀 Как протестировать:

### Шаг 1: Перезапустить AI Service

```bash
cd /Users/daryak/Desktop/ninja/ai-service

# Остановить если запущен (Ctrl+C)

# Перезапустить
source venv/bin/activate
uvicorn src.api:app --reload
```

### Шаг 2: Перезапустить Backend

```bash
cd /Users/daryak/Desktop/ninja/backend

# Остановить если запущен (Ctrl+C)

# Пересобрать
npm run build

# Запустить
npm run dev
```

### Шаг 3: Тест в Telegram

Отправьте боту команду:
```
/admin_reanalyze [ID проекта]
```

## 🎯 Ожидаемые результаты:

### ✅ Правильно:

```
📍 Генерация статики для выбора визуала
Отправили статику 24.12, ждем обратную связь от клиента, согласование перенесено на 12-13 января

📋 Нет информации:
- Подбор музыки
- Монтаж
- Анимация пэкшотов
```

### ❌ Неправильно (старое поведение):

```
📍 Анимация пэкшотов
Задача "Анимация пэкшотов" не упоминается в переписке.

✅ Согласовано:
- Генерация статики для выбора визуала  <-- ОШИБКА! Не согласовано, ждем ОС
```

## 💰 Стоимость:

⚠️ **GPT-4o дороже чем gemini-2.5-flash-lite**

Примерная стоимость на OpenRouter:
- GPT-4o-mini: ~$0.15 за 1M токенов
- GPT-4o: ~$2.50 за 1M токенов (в 17 раз дороже)
- Claude 3.5 Sonnet: ~$3.00 за 1M токенов

Средний анализ проекта:
- ~50 сообщений = ~5000 токенов
- ~20 блоков = ~40000 токенов для промптов
- **Итого: ~$0.10-0.15 за полный анализ проекта**

## 🔄 Альтернативные модели:

Если GPT-4o слишком дорого, можно попробовать:

```bash
# В файле ai-service/.env изменить:
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet  # Баланс цена/качество
# или
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free  # Бесплатно
```

## 📝 Логи для проверки:

При запуске с `LOG_LEVEL=debug` вы увидите:
```
[debug] 🔍 Checking role for sender_id: 466891805
[debug] ✓ Found client: Ольга Спортмастер
```

## ❓ Если проблемы сохранились:

1. Убедитесь что AI service перезапущен
2. Проверьте что в `.env` стоит `openai/gpt-4o`
3. Проверьте логи AI service на ошибки
4. Попробуйте Claude 3.5 Sonnet вместо GPT-4o
